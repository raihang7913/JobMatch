{
  "name": "job-search-app",
  "version": "1.0.0",
  "description": "AI-powered job search platform with CV matching for Indonesian job market",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && .venv\\Scripts\\python.exe main.py",
    "frontend": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && python -m venv .venv && source .venv/Scripts/activate && pip install -r requirements.txt && cd ../frontend && npm install",
    "build": "concurrently \"npm run build:backend\" \"npm run build:frontend\"",
    "build:backend": "echo 'Backend is runtime-only (Python/FastAPI)'",
    "build:frontend": "cd frontend && npm run build",
    "test": "cd backend && source .venv/Scripts/activate && pytest test_basic.py -v"
  },
  "devDependencies": {
    "concurrently": "^9.2.3"
  }
}
